import Button from 'react-bootstrap/Button';

const ContactCard = ({}) => {
    // Hold..

    return (
        <div>
            <Button className='m-3' variant='outline-dark'>Contact Card</Button>
        </div>
    );
};

export default ContactCard;